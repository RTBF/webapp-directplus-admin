// Generated by CoffeeScript 1.4.0
var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define(['jquery', 'backbone', 'backbonels', 'application/views/slideScreen', 'application/models/slide'], function($, Backbone, Backbonels, slideScreen, Slide) {
  var slides;
  return slides = (function(_super) {

    __extends(slides, _super);

    function slides() {
      return slides.__super__.constructor.apply(this, arguments);
    }

    slides.prototype.model = Slide;

    slides.prototype.localStorage = new Backbonels("slidesStore");

    slides.prototype.restore = function() {
      return this.each(function(slide) {
        var slideView;
        slideView = new slideScreen({
          model: slide
        });
        return $('#SlideList').append(slideView.render().el);
      });
    };

    slides.prototype.showLast = function() {
      var lastSlide;
      this.each(function(slide) {
        console.log(slide.id);
        return $('#' + slide.id).hide();
      });
      lastSlide = this.at(this.length - 1);
      return $('#' + lastSlide.id).show();
    };

    return slides;

  })(Backbone.Collection);
});

// Generated by CoffeeScript 1.4.0
var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define('app/models/todo', ['ember'], function() {
  var todo;
  return todo = (function(_super) {

    __extends(todo, _super);

    todo.prototype.id = null;

    todo.prototype.title = null;

    todo.prototype.completed = false;

    todo.prototype.store = null;

    function todo(args) {
      todo.__super__.constructor.call(this, args);
      this.addObserver('title', this.todoChanged);
      this.addObserver('completed', this.todoChanged);
    }

    todo.prototype.todoChanged = function() {
      return this.get('store').update(this);
    };

    return todo;

  })(Ember.Object);
});

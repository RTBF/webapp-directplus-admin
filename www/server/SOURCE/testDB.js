// Generated by CoffeeScript 1.4.0
var Kitten, db, kittySchema, mongoose;

mongoose = require('mongoose');

mongoose.connect('mongodb://localhost/Testingdeux');

db = mongoose.connection;

db.on('error', console.error.bind(console, 'connection error:'));

kittySchema = mongoose.Schema({
  name: String
});

Kitten = mongoose.model('Kitten', kittySchema);

db.once('open', function() {
  var fluffy, silence;
  silence = new Kitten({
    name: 'Silence'
  });
  fluffy = new Kitten({
    name: 'fluffy'
  });
  fluffy.save(function(err, fluffy) {
    if (err) {
      return console.log(" save erreur man");
    }
  });
  silence.save(function(err, silence) {
    if (err) {
      return console.log("save erreur man");
    }
  });
  return Kitten.find(function(err, kittens) {
    if (err) {
      console.log("find erreur man");
    }
    return console.log(kittens);
  });
});
